version: '3.8'

services:
  api:
    build: .
    container_name: talent-classifier-api
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./models:/app/models
    environment:
      - HOST=0.0.0.0
      - PORT=8000
      - WORKERS=1
    restart: unless-stopped
